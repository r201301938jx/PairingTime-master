<% provide(:title, "会員検索結果") %>

<div class="row">
  <%= render 'search_form' %>
</div>

<div class="col-12 col-xs-12 col-sm-12">
  <h2 class="heading text-center">「<%= @content %>」と一致した会員一覧（全<%= @all_records.count %>件）</h2>
  <%= render 'index', customers: @records %>
</div>