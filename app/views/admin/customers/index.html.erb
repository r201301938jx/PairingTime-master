<% provide(:title, "会員一覧") %>

<div class="row">
  <%= render 'sort_form' %>
  <%= render 'search_form' %>
</div>

<div class="col-12 col-xs-12 col-sm-12">
  <h2 class="heading text-center">会員一覧（全<%= Customer.count %>件）</h2>
  <%= render 'index', customers: @customers %>
</div>