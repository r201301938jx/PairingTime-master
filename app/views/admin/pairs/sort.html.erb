<% provide(:title, "投稿一覧") %>

<div class="row">
  <%= render 'sort_form' %>
  <%= render 'search_form' %>
</div>

<div class="col-12 col-xs-12 col-sm-12">
  <h2 class="heading text-center">投稿一覧（全<%= @all_pairs.count %>件）</h2>
  <%= render 'index', pairs: @pairs %>
</div>